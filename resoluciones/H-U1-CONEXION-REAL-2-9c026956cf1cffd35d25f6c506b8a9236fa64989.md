# Resolución auditora — segunda conexión real de U1

WORK_ID=ideas-audiencia-ia
CARRIL=Z
UNIDAD=unidad-1-viabilidad-ia-contratada
INCOMING_TURN_ID_DE_REANUDACION=15

WORK_REPO=francogg89-ai/work-claude-z
WORK_SHA_CONGELADO=cb023ed6c53dd63470da9463a6ea1697294f1055
CONTRATOS_PATH=unidad-1-viabilidad-ia-contratada/EVENTO.md
CONTRATOS_BLOB_SHA=606a381320675f1d71b014ab504da30e1468e946
CHECKPOINT_PATH=unidad-1-viabilidad-ia-contratada/CHECKPOINT_HUMANO.md
CHECKPOINT_BLOB_SHA=b3dcb702deec3cc9656eb4ec5025c8a515c96b1d
AUDIT_FREEZE_SHA=7e07a0100295758cd169d410cdfbee3d18123acb

EVIDENCE_REPO=francogg89-ai/error111
EVIDENCE_SHA=9c026956cf1cffd35d25f6c506b8a9236fa64989
EVIDENCE_PATH=evidencia Z - segunda conexion U1
EVIDENCE_RESOLUTION_PATH=evidencia Z - segunda conexion U1/RESOLUCION_EVIDENCIA.md
EVIDENCE_SERVER_PATH=evidencia Z - segunda conexion U1/evidencia-servidor.json

PERIMETRO_ULTIMA_MODIFICACION=CONSTITUCION
VEREDICTO=CONTRATOS_C_U1_2A_Y_C_U1_2B_EJECUTADOS_CON_EXITO
CONTRATO_C_U1_2A_AGOTADO=SI
CONTRATO_C_U1_2B_AGOTADO=SI
RESULTADO_C_U1_2A=EXITO
RESULTADO_C_U1_2B=EXITO
HUMAN_NEED_RESUELTA=SI
U1_CERRADA=NO

## Resolución humana preservada

El humano devolvió la evidencia material solicitada para la segunda conexión real de U1 en la identidad exacta `francogg89-ai/error111 @ 9c026956cf1cffd35d25f6c506b8a9236fa64989`, bajo `evidencia Z - segunda conexion U1`.

La ejecución declara y documenta:

- cuenta `ChatGPT Plus personal`;
- administrador no requerido;
- Quick Tunnel de Cloudflare aceptado únicamente durante la prueba;
- candidato exacto `work-claude-z @ cb023ed6c53dd63470da9463a6ea1697294f1055`;
- G-1 satisfecha sobre ese corte;
- G0 con Python 3.12.4 y `60 passed in 18.03s`;
- G1 con la sonda iniciada mediante `--fresh`;
- G2 satisfecha con las cinco herramientas, `404` sin token y `404` con capacidad inválida;
- Developer mode activo;
- app válida creada por URL pública y `No Authentication`;
- ninguna confirmación solicitada durante creación, conexión, lectura, vista, escritura o control inválido;
- ningún límite de uso de ChatGPT observado;
- ninguna incidencia reportada de exposición de token o URL portadora fuera del campo de ChatGPT.

La URL portadora y el token no están contenidos en la evidencia durable revisada.

## Repetición admitida

El checkpoint congelado permitía una segunda conversación únicamente si en la primera ChatGPT respondía alguna petición sin mostrar una llamada a la app. Durante la primera conversación el ejecutor interpretó inicialmente que las peticiones 1 y 4 no mostraban llamada; luego identificó la atribución a `Sonda propuestas U1 v2` en el panel `Sources`.

Se realizó una única repetición completa antes del marcador de capacidad inválida. No se usa esa repetición para rescatar un criterio fallido: la primera conversación, por sí sola, contiene evidencia suficiente para satisfacer C-U1-2A y C-U1-2B. Por tanto la repetición no altera el veredicto de los contratos; queda preservada como evidencia adicional y explica que existan dos evaluaciones almacenadas.

## C-U1-2A — integración

El contrato se interpreta exactamente contra sus criterios congelados.

### E1 — SATISFECHO

La primera conversación registra `list_proposals` con `limit=100`. La evidencia humana informa 100 propuestas, primera `P-001` y última `P-100`.

### E2 — SATISFECHO

La primera conversación registra `show_proposal` para `P-042`, `ok=true`, con `original_fp=bac7f90c`. La evidencia humana informa que ChatGPT afirmó que no había datos de contacto.

El texto libre del modelo vuelve a normalizar `[SINTETICO]` como `[SINTÉTICO]`; esto no incumple C-U1-2A porque el contrato congelado ya documentó ese límite y trasladó la literalidad de presentación a C-U1-2B. La integración sí recuperó el original exacto del lado servidor, que es la propiedad de E2 en A.

### E3 — SATISFECHO

La primera conversación registra `save_evaluation` de `P-042` con `result=duda`, `reasons=prueba real C1.3`, `doubts=ninguna`, seguida de `get_evaluations`, ambas `ok=true`. La evaluación quedó almacenada.

### E4 — SATISFECHO

Entre `inicio-intento-capacidad-invalida` y `fin` el servidor registra dos `POST` y un `GET`, todos con `path_ok=false` y respuesta `404`, ninguna solicitud aceptada y ninguna llamada a herramientas. La segunda app no llegó a crearse ni obtuvo herramientas o datos.

### E5 — SATISFECHO

La primera conversación registra `show_proposal` de `X-001` con `ok=false`; la respuesta humana preservada informa que `X-001` no está disponible y no reproduce su contenido.

### E6 — SATISFECHO

Todas las solicitudes con ruta correcta y respuesta `2xx` entre `inicio-chatgpt` e `inicio-intento-capacidad-invalida` tienen `User-Agent` que comienza con `openai-mcp/`. El registro incluye `server/discover`, `tools/list`, `resources/read` y `tools/call`; no aparece otro cliente aceptado en esa ventana.

### Resultado A

C-U1-2A exige E1 a E6. Todos quedan satisfechos en la primera conversación, por lo que:

`RESULTADO_C_U1_2A=EXITO`.

Este éxito sostiene, dentro del alcance congelado, que M2 permitió en la cuenta ChatGPT Plus personal declarada el viaje real de lectura, recuperación del original exacto del lado de la integración, escritura y recuperación posterior, con controles negativos de acceso y alcance e integridad de evidencia. No se generaliza a otras cuentas, planes ni IA.

La evidencia vuelve a mostrar que la escritura se ejecutó sin confirmación. Por tanto cualquier acción de producto que requiera autorización humana no puede descansar en una confirmación automática de ChatGPT; esta limitación permanece relevante para U2.

## C-U1-2B — vista fiel del original

### B1 — SATISFECHO

La primera conversación registra `show_proposal` de `P-042`, `ok=true`, con `original_fp=bac7f90c`. El servidor registra además solicitudes `resources/read` aceptadas con `User-Agent: openai-mcp/1.0.0`, por lo que el anfitrión solicitó el recurso UI.

### B2 — SATISFECHO

La evidencia humana preserva `03-peticion-3-vista.png` y transcribe literalmente, dentro de la vista renderizada de la app:

- `Huella de lo mostrado: bac7f90c`
- `Huella del servidor: bac7f90c`
- `Coinciden: SI`

La resolución humana declara que la vista se distingue de la respuesta del modelo. La captura queda preservada en el mismo commit de evidencia y la transcripción literal forma parte de `RESOLUCION_EVIDENCIA.md`.

Como contraste observable, el propio texto libre del modelo vuelve a escribir `[SINTÉTICO]`, mientras la vista muestra `[SINTETICO]` y las dos huellas coinciden. Esto es consistente con la propiedad que B debía discriminar: la vista entrega un canal de presentación fiel independiente de la regeneración del modelo.

### Resultado B

C-U1-2B exige B1 y B2. Ambos quedan satisfechos, por lo que:

`RESULTADO_C_U1_2B=EXITO`.

Este éxito sostiene, para la cuenta y candidato ejecutados, que la vista MCP Apps ofrece dentro de ChatGPT un canal fiel para mostrar el original de `P-042` tal como fue recibido, sin depender de la transcripción del modelo. No se extiende esa conclusión más allá del alcance congelado.

## Control inválido y causalidad

La segunda app con capacidad inválida fue rechazada con el mensaje genérico `Error creating connector / Something went wrong...`. Como en la primera corrida, ese mensaje no atribuye la causa a plan, cuenta, política, rol ni permisos. No se usa para inferir indisponibilidad de capacidad.

El control sí cumple la función negativa fijada: hubo tráfico real de OpenAI hacia la ruta inválida, todas las respuestas fueron `404`, no hubo rutas aceptadas ni llamadas a herramientas y no se obtuvieron datos.

## Manejo del secreto

La corrección introducida después de la primera corrida funcionó según la evidencia reportada: la URL portadora se trasladó mediante `probe.url copiar`/`copiar-invalida`, se pegó únicamente en ChatGPT y se limpió inmediatamente con `probe.url limpiar`. No se reportó exposición de la URL portadora ni del token al agente auxiliar.

E6 sólo detecta contaminación accidental y no constituye autenticación fuerte; esa limitación ya estaba congelada y no se amplía por este resultado.

## Suficiencia y naturaleza de la evidencia

`evidencia-servidor.json` fue inspeccionado directamente por el AUDITOR y confirma 28 solicitudes, 12 llamadas a herramientas, dos evaluaciones, las huellas `bac7f90c`, `resources/read`, los controles negativos y los `User-Agent` esperados.

Las capturas y transcripciones fueron producidas por la ejecución humana y se clasifican como evidencia material reportada. Los archivos de captura están preservados en el commit exacto de evidencia; la resolución durable contiene la transcripción literal requerida por B2.

No se redefine ningún criterio después de observar la corrida.

## Estado de U1 después de los contratos

Ambos contratos quedaron agotados al producir resultado. Una nueva corrida discriminante requeriría un contrato nuevo conforme a REVOLUTIONS §6.1.

C-U1-2A aporta la evidencia de `opción demostrada` que U1 necesitaba para el mecanismo M2 en la cuenta de referencia. C-U1-2B demuestra además el canal de presentación fiel requerido para no depender del texto libre del modelo.

Esto deja a U1 técnicamente encaminada a cierre, pero esta intervención auditora no declara todavía `U1_CERRADA=SI`: el resultado debe volver al CONSTRUCTOR para que lo preserve en el material de U1, consolide requisitos, límites, costos/consumo observados y acciones manuales, y prepare un candidato de cierre de unidad. Ese candidato deberá volver al AUDITOR. La constitución mantiene separadas la aprobación de cierre de U1 y la autorización humana para iniciar U2.

## Próxima acción

La necesidad humana `H-U1-CONEXION-REAL-2` queda resuelta. El loop vuelve al CONSTRUCTOR current.

El CONSTRUCTOR debe rederivar desde Git, tratar C-U1-2A y C-U1-2B como agotados con `EXITO`, integrar durablemente la evidencia exacta `francogg89-ai/error111 @ 9c026956cf1cffd35d25f6c506b8a9236fa64989`, preservar las limitaciones observadas —incluida la regeneración no literal del texto del modelo y la ausencia de confirmación antes de escritura—, y preparar el candidato de cierre de U1 sin iniciar U2. No debe ejecutar otra verificación discriminante ni modificar manifiesto, PLAN.md o perímetro sin autoridad correspondiente.